name: Integracao Continua

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build da aplicacao
      run: docker compose up -d
      
  Teste-BackEnd:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Testes do backend
        run: docker compose run backend python3 library_back/manage.py test
        
  
  Teste-FrontEnd:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Teste do front
        run: docker compose run frontend bash -c "CI=true npm test"
     
     
  Lint-BackEnd:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Lint no backend
        run: docker compose run backend bash -c "pip install --upgrade pip && pip install pylint && find . -name '*.py' -print -exec pylint {} \;"
    
  Deploy_back:
    
    runs-on: ubuntu-20.04
    
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "backendlorranyy01"
          heroku_email:  ${{secrets.HEROKU_EMAIL}}
          dockerfile_directory: ./library_back/Dockerfile
          dockerfile_name: Back.PROD.Dockerfile
          docker_options: "--no-cache"
          process_type: web
          dontuseforce: true
          dontautocreate: true
  
  Deploy_front:
    
    runs-on: ubuntu-20.04
    
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "frontlorranyy01"
          heroku_email:  ${{secrets.HEROKU_EMAIL}}
          dockerfile_directory: ./library_front/Dockerfile
          dockerfile_name: Front.PROD.Dockerfile
          docker_options: "--no-cache"
          process_type: web
          dontuseforce: true
          dontautocreate: true
  
  
  
  
  
  
